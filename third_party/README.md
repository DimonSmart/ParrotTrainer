# Vendored Flutter plugins

These packages are local copies of their latest published versions.

They remove direct Kotlin Gradle Plugin application so the Android build can use Flutter's built-in Kotlin support. Keep the changes when updating either package until its published version includes the same migration.
